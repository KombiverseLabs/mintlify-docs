---
title: "Install SpeechKit on Windows"
description: "Download and verify the SpeechKit Windows beta"
sidebarTitle: "Install on Windows"
icon: "windows"
---

The SpeechKit desktop client supports Windows 10 and Windows 11 on x64
systems. Windows is the complete target: all three modes, Hands-Free, and
Meeting. Download assets only from the official `kombifyio/SpeechKit` release.

{/* speechkit-release:begin */}
The current public release is [v0.72.1](https://github.com/kombifyio/SpeechKit/releases/tag/v0.72.1), published 2026-09-20.
{/* speechkit-release:end */}

## Choose a package

<CardGroup cols={2}>
  <Card title="Windows installer" icon="download" href="https://github.com/kombifyio/SpeechKit/releases/latest/download/SpeechKit-Setup.exe">
    Use `SpeechKit-Setup.exe` for the standard installation path.
  </Card>
  <Card title="Portable ZIP" icon="file-zipper" href="https://github.com/kombifyio/SpeechKit/releases/latest/download/SpeechKit-Portable.zip">
    Extract `SpeechKit-Portable.zip` when you do not want an installer.
  </Card>
</CardGroup>

Both links always resolve to the newest published release.

## Verify the download

1. Download the matching
   [SHA256SUMS.txt](https://github.com/kombifyio/SpeechKit/releases/latest/download/SHA256SUMS.txt).
2. Calculate the SHA-256 hash of the installer or ZIP with a tool you trust.
3. Compare the complete hash with the entry in `SHA256SUMS.txt` before opening
   the package.
4. Read the
   [unsigned-release notice](https://github.com/kombifyio/SpeechKit/releases/latest/download/UNSIGNED-WINDOWS-RELEASE.txt).

<Warning>
The no-cost Windows release is unsigned. Windows may display a reputation
warning. Verify the checksum and release origin before deciding whether to
continue.
</Warning>

## First use

1. Start SpeechKit and select the microphone you want it to use.
2. Choose a Network Scope. All networks allows external providers; local
   network only and this device only restrict what the backend may contact.
3. Choose a local or external provider appropriate for the mode.
4. Test Dictation before enabling Assist, Voice Agent, or Meeting.
5. Review what leaves the device when selecting an external provider.

The default hotkeys are `Ctrl+Win` for Dictation, `Win+Alt` for Assist, and
`Ctrl+Shift` for Voice Agent. Inside a meeting, `Ctrl+Alt+S` captures a screen
snapshot.

<CardGroup cols={2}>
  <Card title="Understand modes" icon="waveform-lines" href="/speechkit/modes">
    Learn what each mode does and how provider choice changes the data path.
  </Card>
  <Card title="Record a meeting" icon="users" href="/speechkit/meeting">
    Capture a call on two channels and generate a cited write-up.
  </Card>
  <Card title="Release notes" icon="clock-rotate-left" href="https://github.com/kombifyio/SpeechKit/releases/latest">
    Review the current release and all published assets.
  </Card>
  <Card title="macOS beta" icon="apple" href="/speechkit/install-macos">
    Install the Apple Silicon Dictation beta instead.
  </Card>
</CardGroup>
