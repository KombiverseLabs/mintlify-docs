---
title: kombify Cloud
description: Manage your homelabs from anywhere — no setup required
sidebarTitle: Overview
icon: globe
---


**kombify Cloud** is the easiest way to manage your homelab. Sign up, connect your servers, and you're done — we handle the rest.

## The simple version

<Card title="What is Cloud?" icon="lightbulb">
  Cloud is like having a professional IT team for your homelab. It provides a web dashboard where you can manage all your servers, deploy services, and invite team members — without setting up any infrastructure yourself.
</Card>

## Self-hosted vs. managed

<Tabs>
  <Tab title="Self-hosted (Stack only)">
    **You manage everything:**
    - ✅ Complete control
    - ✅ No subscription costs
    - ❌ You handle updates and security patches
    - ❌ You configure backups
    - ❌ You set up user management
    - ❌ One homelab at a time
  </Tab>
  <Tab title="Managed (Cloud)">
    **We handle the complexity:**
    - ✅ Automatic updates and security
    - ✅ Built-in backups
    - ✅ User management and SSO
    - ✅ Manage multiple homelabs from one place
    - ✅ Team collaboration built-in
    - ✅ Support when you need it
  </Tab>
</Tabs>

<Accordion title="🔧 Technical details" icon="code">
  Cloud is a multi-tenant SaaS platform built with:
  - **Frontend:** SvelteKit with real-time updates
  - **Backend:** Go API with PostgreSQL 17 and Prisma ORM
  - **Identity:** Auth0 for OIDC/SAML/passkey authentication
  - **Billing:** Stripe for subscriptions
  - **Agent:** Lightweight Go binary with mTLS for secure homelab connectivity
  
  Your data stays on your servers — Cloud only stores configuration and metadata.
</Accordion>

## How it works

```mermaid
flowchart TB
    subgraph You["👤 You & Your Team"]
        Admin["Admin"]
        Dev["Developer"]
        Viewer["Viewer"]
    end
    
    subgraph Cloud["☁️ kombify Cloud"]
        Dashboard["🖥️ Dashboard"]
        Auth["🔐 Login<br/><small>Passkeys, SSO</small>"]
        API["🔌 API"]
        Billing["💳 Billing"]
    end
    
    subgraph Homelabs["🏠 Your Homelabs"]
        HL1["<b>Production</b><br/>3 nodes"]
        HL2["<b>Development</b><br/>1 node"]
        HL3["<b>Testing</b><br/>2 nodes"]
    end
    
    You -->|"Secure login"| Auth
    Auth --> Dashboard
    Dashboard --> API
    API --> Billing
    API <-->|"Encrypted tunnel"| Homelabs
    
    style You fill:#1e293b,stroke:#38bdf8,color:#f8fafc
    style Cloud fill:#1e293b,stroke:#fbbf24,color:#f8fafc
    style Homelabs fill:#1e293b,stroke:#4ade80,color:#f8fafc
```

### What stays where?

| Data | Location | Why |
|------|----------|-----|
| **Your files & databases** | Your servers | Your data never leaves your infrastructure |
| **Configuration** | Cloud cloud | So you can access it from anywhere |
| **User accounts** | Cloud cloud | Managed securely by Auth0 |
| **Deployment history** | Cloud cloud | Track who changed what, when |

## Key features

<CardGroup cols={3}>
  <Card title="Multi-homelab" icon="server">
    Manage production, development, and testing environments from one dashboard
  </Card>
  <Card title="Team access" icon="users">
    Invite team members with role-based permissions
  </Card>
  <Card title="Passwordless login" icon="fingerprint">
    Passkeys and SSO — no passwords to remember
  </Card>
  <Card title="Automatic backups" icon="cloud-arrow-up">
    Configuration backed up automatically
  </Card>
  <Card title="Usage insights" icon="chart-line">
    See how your infrastructure is performing
  </Card>
  <Card title="Priority support" icon="headset">
    Get help when you need it
  </Card>
</CardGroup>

## Architecture

```mermaid
graph TB
    subgraph Cloud["☁️ Cloud Cloud"]
        subgraph Frontend["Frontend"]
            SvelteKit["🖥️ Dashboard<br/><small>SvelteKit</small>"]
        end
        
        subgraph Backend["Backend"]
            API["🔌 API<br/><small>Go</small>"]
            Auth["🔐 Identity<br/><small>Auth0</small>"]
            PB["💾 State<br/><small>PostgreSQL + Prisma</small>"]
        end
        
        subgraph Integrations["Integrations"]
            Stripe["💳 Payments<br/><small>Stripe</small>"]
            SMTP["📧 Email"]
        end
    end
    
    subgraph OnPrem["🏠 Your Premises"]
        Agent1["🤖 Agent<br/><small>Homelab 1</small>"]
        Agent2["🤖 Agent<br/><small>Homelab 2</small>"]
    end
    
    SvelteKit --> API
    API --> Auth
    API --> PB
    API --> Stripe
    API --> SMTP
    
    API <-->|"mTLS encrypted"| Agent1
    API <-->|"mTLS encrypted"| Agent2
    
    style Cloud fill:#1e293b,stroke:#38bdf8,color:#f8fafc
    style OnPrem fill:#1e293b,stroke:#4ade80,color:#f8fafc
```

<Accordion title="🔧 How the agent connection works" icon="link">

```mermaid
sequenceDiagram
    participant Homelab as Your Server
    participant Agent as Cloud Agent
    participant Cloud as Cloud Cloud
    
    Note over Agent,Cloud: Initial Setup (once)
    Agent->>Cloud: Register with pairing token
    Cloud->>Agent: Issue mTLS certificate
    
    Note over Agent,Cloud: Normal Operation
    loop Every 30 seconds
        Agent->>Cloud: Heartbeat + metrics
        Cloud->>Agent: Any pending commands?
    end
    
    Note over Agent,Cloud: Deployment
    Cloud->>Agent: Deploy command
    Agent->>Homelab: Execute deployment
    Agent->>Cloud: Report status
```

**Key points:**
- Agent connects **outbound** — no inbound ports needed on your network
- All communication is **mTLS encrypted** — both sides verify each other
- Agent is a **lightweight Go binary** (~15MB) that runs as a systemd service

</Accordion>

### SSO-Based Tool Access

kombify Cloud provides seamless authenticated access to all kombify tools via token-based SSO. Users authenticate once via Auth0 on [kombify.io](https://kombify.io), and get automatic access to their TechStack, Simulate, and AI instances without additional login steps.

<Steps>
  <Step title="Sign up">
    Create your account at [kombify.io](https://kombify.io).
  </Step>

  <Step title="Create organization">
    Set up your organization and configure your subscription plan.
  </Step>

  <Step title="Add team members">
    Invite team members and assign roles for access control.
  </Step>

  <Step title="Launch tools">
    Access TechStack, Simulate, and AI directly from the unified dashboard -- SSO handles authentication automatically.
  </Step>
</Steps>

## Team & Access Control

### Role-Based Access

| Role | View | Deploy | Configure | Billing | Admin |
|------|:----:|:------:|:---------:|:-------:|:-----:|
| Viewer | ✅ | ❌ | ❌ | ❌ | ❌ |
| Operator | ✅ | ✅ | ❌ | ❌ | ❌ |
| Developer | ✅ | ✅ | ✅ | ❌ | ❌ |
| Admin | ✅ | ✅ | ✅ | ✅ | ✅ |
| Owner | ✅ | ✅ | ✅ | ✅ | ✅ |

### SSO Configuration

Cloud uses Auth0 for enterprise-grade authentication:

```mermaid
sequenceDiagram
    participant User
    participant Cloud
    participant Auth0
    participant IdP as Your IdP (Optional)
    
    User->>Cloud: Access dashboard
    Cloud->>Auth0: Redirect to login
    
    alt Native Login
        Auth0->>User: Show login form
        User->>Auth0: Enter credentials
    else Enterprise SSO
        Auth0->>IdP: Redirect to corporate IdP
        User->>IdP: Authenticate
        IdP->>Auth0: Return assertion
    end
    
    Auth0->>Cloud: Return tokens
    Cloud->>User: Authenticated session
```

Supported identity providers:
- **OIDC**: Any OIDC-compliant provider
- **SAML**: Okta, Azure AD, OneLogin, etc.
- **LDAP**: Active Directory, OpenLDAP
- **Social**: GitHub, Google, Microsoft

## Pricing Plans

<CardGroup cols={3}>
  <Card title="Free" icon="gift">
    **$0/month**

    - 1 connected homelab
    - 1 team member
    - Community support
  </Card>

  <Card title="Pro" icon="bolt">
    **$15/month**

    - 5 connected homelabs
    - 5 team members
    - Email support
    - All tools included
  </Card>

  <Card title="Team" icon="users">
    **$49/month**

    - Unlimited homelabs
    - 25 team members
    - Priority support
    - SSO (OIDC)
  </Card>
</CardGroup>

<Note>
  All plans include a 14-day free trial. No credit card required.
</Note>

## Dashboard Features

### Homelab Overview

View all your homelabs at a glance:
- Node health status
- Resource utilization
- Recent deployments
- Active alerts

### Deployment History

Track all changes:
- Who deployed what
- When it was deployed
- Rollback capability
- Diff viewer

### Monitoring & Alerts

Built-in monitoring:
- Node availability
- Resource usage
- Service health
- Custom alert rules

## Security

<AccordionGroup>
  <Accordion title="Data Encryption" icon="lock">
    - All data encrypted at rest (AES-256)
    - All traffic encrypted in transit (TLS 1.3)
    - mTLS for agent communication
  </Accordion>
  
  <Accordion title="Access Control" icon="shield">
    - Role-based access control
    - Audit logging for all actions
    - IP allowlist support (Team plan)
  </Accordion>
  
  <Accordion title="Compliance" icon="clipboard-check">
    - SOC 2 Type II (in progress)
    - GDPR compliant
    - Data residency options (EU/US)
  </Accordion>
</AccordionGroup>

## Self-Hosted vs Cloud

| Feature | Self-Hosted (Stack) | Managed (Cloud) |
|---------|---------------------|------------------|
| **Cost** | Free | $0-49/month |
| **Setup** | ~1 hour | ~5 minutes |
| **Updates** | Manual | Automatic |
| **Multi-homelab** | No | Yes |
| **Team access** | DIY | Built-in |
| **SSO** | DIY | Included |
| **Backups** | DIY | Automatic |
| **Support** | Community | Priority |

## Getting Started

<Steps>
  <Step title="Create account">
    Sign up at [kombify.io](https://kombify.io)
  </Step>
  
  <Step title="Add your first homelab">
    Follow the guided setup to connect your infrastructure
  </Step>
  
  <Step title="Deploy your first stack">
    Choose a StackKit and deploy in minutes
  </Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Quickstart Guide" icon="rocket" href="/quickstart">
    Get up and running in 5 minutes
  </Card>
  <Card title="Agent Installation" icon="download" href="/cloud/overview">
    Detailed agent setup instructions
  </Card>
  <Card title="SSO Setup" icon="key" href="/cloud/enterprise">
    Configure enterprise authentication
  </Card>
  <Card title="API Reference" icon="code" href="/api/kombifycloud/overview">
    Automate with the Cloud API
  </Card>
</CardGroup>
