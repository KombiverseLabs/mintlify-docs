---
title: "Homepage"
description: "A released navigation module outside the initial native v2 StackSpec"
sidebarTitle: "Homepage"
icon: house
---

Homepage ships as a v0.16.0 module contract, but it is not selected by the initial native v2 StackSpec. The Node Hub is the current generated core entrypoint.

Use it for daily navigation. Use the Node Hub and CLI output for first-run state, recovery material, and verification details.

```bash
stackkit status --json
stackkit verify --json
```

Do not invent a Homepage module selection from an older schema. If a future StackSpec exposes it, use only the installed release's validated fields and generated route.
