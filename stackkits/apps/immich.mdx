---
title: "Immich"
description: "Use the released StackKits photo and video application"
sidebarTitle: "Immich"
icon: images
---

Immich is the default photo and video application in the current released StackKits catalog. When included in your stack, its generated route is normally `photos.<domain>`.

## First use

1. Complete `stackkit verify --json` after the rollout.
2. Open the generated Immich address from the Node Hub or rollout output.
3. Create the application owner account and connect clients only after you have confirmed storage capacity and backups.

Photo libraries grow quickly. Keep the storage and backup responsibility with the host you operate, and verify the StackKits workspace after any configuration change.

See [Family Photo Vault](/guides/stackkits/use-cases/family-photo-vault) for a standalone use-case guide.
