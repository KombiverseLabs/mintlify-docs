---
title: "OS compatibility"
description: "Lifecycle evidence for StackKits v0.35.0"
sidebarTitle: "OS compatibility"
icon: server
generated: true
generated_by: "stackkit docs sync-release-manifests"
content_hash: "sha256:27454366b9b2240f4b19e04fe4fb86d00cf4843cd245f5c4c7f7989ea0e5fe3a"
source_hash: "0badfb07fdb2462ca921a8b521914643362d6f43"
---

Rows are projected from the automated lifecycle runs of StackKits [v0.35.0](https://github.com/kombifyio/StackKits/releases/tag/v0.35.0) on fresh virtual machines: install, init, generate, apply, verify, backup and restore. Last lifecycle run: 2026-09-17.

- `supported`: Every lifecycle phase passed in the newest run on this release.
- `preview`: Install through verify passed in the newest run; a later phase failed.
- `unverified`: No completed run on this release yet, or the newest run failed before verify.

Operating-system and hypervisor rows show the best result across the environments tested on this release; Kits by environment lists each environment on its own.

| Operating system | Version | Tested architecture | Status | Evidence | Last verified |
| --- | --- | --- | --- | --- | --- |
| Ubuntu | 24.04 | amd64 | `supported` | All lifecycle phases passed | v0.35.0 |
| Debian | 13 | amd64 | `supported` | All lifecycle phases passed | v0.35.0 |
| Debian | 12 | — | `unverified` | Not covered by the automated lifecycle tests yet | — |

Each run installs the operating system fresh in a virtual machine.

## Kits by environment

Each kit is graded in the kind of environment it is built for. A home network run also opens the local service addresses from another machine on that network.

| Kit | Environment | Status | Evidence | Last verified |
| --- | --- | --- | --- | --- |
| Basement Kit | Fresh virtual machine in CI | `supported` | All lifecycle phases passed | v0.35.0 |
| Basement Kit | Virtual machine on a home network | `supported` | All lifecycle phases passed; local service addresses answered from the home network | v0.35.0 |
| Cloud Kit | Public VPS with a kombify.me address | `unverified` | Not covered by the automated lifecycle tests yet | — |
| Modern Homelab | Home virtual machine plus public VPS | `unverified` | Not covered by the automated lifecycle tests yet | — |

## Hypervisors

StackKits run inside a guest VM on your hypervisor, never on the hypervisor host itself. A status covers that rollout end to end: the guest is created on the hypervisor and the StackKit lifecycle runs inside it. It does not certify a vendor product or a server provider.

The hypervisor rollouts and their lifecycle evidence for this release are listed on [stackkit.cc/compatibility](https://stackkit.cc/compatibility). Run `stackkit compat` on a host for non-destructive diagnostics and the evidence published for its operating system and hypervisor.
