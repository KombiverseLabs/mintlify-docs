---
title: "Netdata"
description: "Real-time per-node system metrics with Netdata"
sidebarTitle: "Netdata"
icon: gauge
---

Netdata provides real-time, high-resolution system metrics (CPU, RAM, disk, network, per-container) for each node. It is the **default** server-metrics service on standard and high compute tiers.

Netdata is served at `netdata.<domain>`.

## When you get it

Netdata is enabled by the resolver on standard/high tiers. On low-resource nodes the resolver uses lighter tools ([Beszel](/guides/stackkits/services/beszel) or Glances) instead.

```yaml stack-spec.yaml
stackkit: base-kit
# Netdata is enabled by default on standard/high tiers — no config needed.
```

## Steps

<Steps>
  <Step title="Open Netdata">
    Open `https://netdata.<domain>` to see live per-node dashboards.
  </Step>
  <Step title="Review key charts">
    Check CPU, memory pressure, disk I/O, and per-container resource usage.
  </Step>
  <Step title="Set alarms">
    Tune Netdata's built-in health alarms for the thresholds you care about.
  </Step>
</Steps>

## Verify

- Live charts update at `netdata.<domain>`.
- Per-container metrics appear for your running services.

## Next steps

<CardGroup cols={2}>
  <Card title="Uptime Kuma" icon="heart-pulse" href="/guides/stackkits/services/uptime-kuma">
    Add uptime checks and status pages.
  </Card>
  <Card title="Tool alternatives" icon="shuffle" href="/stackkits/reference/tool-alternatives">
    Compare Netdata, Beszel, and Glances.
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Charts empty or node missing">
    Verify the Netdata container is running (`docker logs netdata`) and that the host has access to `/proc` and `/sys`.
  </Accordion>
</AccordionGroup>
