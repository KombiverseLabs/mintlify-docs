---
title: "Private Home Base"
description: "Complete the local StackKits control and identity baseline after the first successful path"
sidebarTitle: "Private Home Base"
icon: house-lock
---

<Info>
  Evidence state (2026-09-17, v0.35.0): Basement Kit is the supported
  one-command path of the current public release. On the public v0.35.0
  archive, Basement Kit passed install, init, generate, apply, verify, backup,
  and restore on fresh Ubuntu 24.04 and Debian 13 virtual machines, and in a
  virtual machine on a home network, where its local service addresses also
  answered from another machine on that network. Files (Cloudreve) passed the
  same lifecycle with setup on Ubuntu 24.04, with the application readback
  verified over the guest-local loopback. Vault (Vaultwarden) passed the same
  lifecycle with setup on Ubuntu 24.04, with restore activation completed and
  its application-level readback after restore pending. Photos (Immich) was
  last verified on v0.33.3; its application-level readback after restore is
  pending. Phone passkey and client certificate trust are
  pending. Cloud Kit is a preview and Modern Homelab an alpha definition
  archive, not an install target; neither has an automated lifecycle run yet.
</Info>

A Private Home Base is the smallest useful Basement Kit: one home site, one local node, local owner custody, routing, identity, the Node Hub, and local backup policy. Add optional workloads only after this baseline verifies.

The first successful path is the [Quickstart](/stackkits/quickstart). Continue here to complete the local baseline.

## Complete the local baseline

1. Open the generated Node Hub address from the verification output.
2. Complete the local PocketID owner setup.
3. Confirm protected routes redirect through TinyAuth.
4. Use the Node Hub protection action after the owner exists.
5. Configure and test the local backup repository.

The baseline remains standalone. The website helps you choose and bootstrap a path, but it does not operate the host.

## Add the first workload

Start with the released Files workload because its standalone selection requires no operator-authored secret reference. See [Private File Library](/guides/stackkits/use-cases/private-file-library).
