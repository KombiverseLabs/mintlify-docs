---
title: "Family Photo Vault"
description: "Run the released Immich application on a StackKits host you control"
sidebarTitle: "Family Photo Vault"
icon: camera-retro
---

A Family Photo Vault is a self-hosted Immich installation on a StackKits host you control. The public StackKits `v0.16.0` catalog lists Immich as a default application.

## Choose the host

- Use [Basement Kit](/stackkits/kits/basement-kit) for local hardware and LAN-first access.
- Use [Cloud Kit](/stackkits/kits/cloud-kit) for an existing VPS or cloud host with a domain you control.

Plan storage and backups before importing a family library. Photos and videos often become the largest data set in a homelab.

## Deploy and verify

```bash
stackkit init basement-kit --owner-source=local
stackkit validate
stackkit generate
stackkit apply
stackkit verify --json
```

Open the generated Immich address only after verification succeeds. Create the application owner account, connect clients, and begin with a small backup set so you can confirm storage growth and recovery expectations.

## Keep it maintainable

- Keep the StackSpec and verification output with the workspace.
- Change the StackSpec rather than generated application files.
- Confirm backups before relying on the service for irreplaceable photos.

For the application-level steps, see [Immich](/stackkits/apps/immich).
